import { PrettyUnitPipe } from './pretty-unit.pipe';

describe('PrettyUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new PrettyUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
